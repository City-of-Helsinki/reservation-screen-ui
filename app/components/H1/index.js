import styled from 'styled-components';

const demoFontSize = '50px';

const H1 = styled.h1`
  font-size: ${demoFontSize};
  line-height: 1;
  margin-top: 0;
  margin-bottom: 10px;
`;

export default H1;
